Gradle - It helps download the required dependencies(Ex: jdbc driver, spring, hibernate).

Install Java,
Download Gradle and install
set environment variables. path - c:\\..\gradle\bin

>gradle -v
to create a project 
>gradle init

gradle.build - It will have the build scripts, written in groovy or kotlin.
>gradle tasks
After build successfull. If javac comiler error - set environment variable JAVA_HOME - c:\programfiles\java\jdk22.1
>gradlew.bat run
